# Local source library

本目录的类型结构由 [`../README.md`](../README.md) 与 [`../catalog.yaml`](../catalog.yaml) 定义。公共 Git 只跟踪本说明；其余第三方原件默认忽略，并由 `scripts/bootstrap`、`scripts/sources sync` 或 `tmp/pdfs` 收件箱恢复。不要在这里建立第二份清单。
